# HANDOFF — HHJ Series + A2G Content Machine

**From:** the Aug 10–11 remote session (cloud container — no vault access).
**To:** a local, vault-aware Claude session on a Mac with `~/OS`.
**Read next:** `ai-movie-journal.md` (the story), `content-machine.md` (the
machine). This file is the bridge; those two are the substance. When the work
lands in its proper homes (vault, Notion, the new repo), this file gets
deleted from the branch — per the supersede-don't-duplicate rule.

---

## Why this handoff exists

The originating session ran remotely and could reach only Notion and this
repo. It could NOT see `~/OS`, so: the Vault Structure Scalability Review
outcome was never read, the health pass (below) could only be prescribed, not
run, and nothing was written to the vault. The local session's first duty is
to close exactly that gap.

## State of play

- **Story: settled.** Harry Hunt Jr's true transformation story (violinist →
  operator building A2G), H2 The Scientist as a character in it, entrepreneur
  audience, comic-panel episodes scored with H2's human-played music. Full
  settled state at the top of `ai-movie-journal.md`.
- **Machine: designed, not built.** Lane 3 story pipeline extending the May 11
  Lane 1 decision; all lanes share the Mini→GHL last mile. Full design in
  `content-machine.md`.
- **Nothing has been built.** No repo created, no code, no assets. By design —
  Harry's directive was to design the whole thing first.

## First job: Step 0 health pass (~30 min, requires the vault)

1. **Find the Vault Structure Scalability Review outcome** in the vault. If it
   changed canonicality or structure rules, `content-machine.md` yields to it —
   reconcile the doc before acting on it.
2. **Canonicality manifest** at the vault root: per domain — system of record,
   working copy, sync direction. Confirmed fact to record while writing it
   (Harry, Aug 11): **all three machines — MacBook Pro, Mac Studio, Mac Mini —
   are vault-synced.** Any of them can host a vault session; role assignments
   (Studio = render appliance, Mini = always-on deploy host) are unchanged.
   Record this in the vault architecture doc so no future session has to
   re-ask.
3. **One NOW per domain** — merge or archive strays in recently-touched domains.
4. **Notion cleanups:** duplicate "Content Pipeline" databases (created Mar 24
   and Apr 5 — pick one, archive the other); Content Console's stale "YouTube
   evaluation gate: Aug 2026" line (the gate is answered — HHJ YouTube becomes
   the weekly teardown channel); Social Media Show Formats project sits Paused
   with overdue reviews but its H2 show definitions are load-bearing — restate
   it as reference material.
5. **Weekly drift check** as a night-shift job (the April "Delta 1 — Vault
   Structure Enforcer" concept, report-only, never auto-deletes).

## Then, in order (detail: `content-machine.md` §10)

1. **Story before software:** series bible v1 + hand-storyboard THREE episodes.
   This is where the title, the goggles-vs-face grammar, the three-character
   relay idea, and the cold-open question get decided — by making pages.
2. **Repo + last mile:** create private `a2g-content-machine`; commit bible,
   prompts, style contracts, schemas. Stand up the still-open Lane 1 v0.1
   prerequisites (Studio/Mini shared folder, GHL Phase 1 for HHJ, Mini deploy
   script with success/failure logging).
3. **Lane 3 skeleton:** memo → transcript → adapt draft → panels → morning
   review folder. Stills only. Seven consecutive unattended nights before
   expanding. H2 asset kit in parallel — Blue Room backplate first, **in its
   gutted state** (the set heals as the real basement rebuild progresses).
4. **Bank ~6 chapters**, then launch short-form + first YouTube teardown in the
   same week.

## Open questions for Harry

1. Three-character relay (H2 builds → student-scientist → executive presents) —
   test in storyboards, not debate.
2. Series title — comes out of storyboarding ("Confessions of a Recovering
   Musician" is the thesis, not the title; Harry's word: *transformational*).
3. GHL sub-account inventory — which brands have sub-accounts + connected
   socials today? Determines Phase 1 effort; check in GHL directly.
4. Scalability-review reconciliation (Step 0.1).

## Provenance

Four commits on `claude/ai-movie-journal-concept-jhd8bs`. Notion sources read:
H2 brand card, Brand Positioning Framework, Social Media Show Formats, the
May 11 Lane 1 decision-log entry, HHJ Content Pipeline v0.1, the Apr 7 journal
entry "Confessions of a Recovering Musician," vault architecture pages. The
line that anchors the whole thesis, from Harry's own April journal:

> "The curiosity didn't die — it found a better puzzle."
